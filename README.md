# HED-Replication
This repository provides a PyTorch-based theoretical replication of Holistically-Nested Edge Detection (HED). The goal is to translate the paper’s deeply-supervised multi-scale architecture into a clean, understandable implementation rather than training a production-ready model.
